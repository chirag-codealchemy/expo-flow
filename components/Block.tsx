import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export const Block = () => {
  return (
    <View>
      <Text>Block</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
